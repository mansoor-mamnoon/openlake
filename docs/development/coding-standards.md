# Documentation Rules

Any change that:

* adds a feature
* changes behavior
* introduces a new concept

must update:

* architecture docs (if structural)
* reference docs (if user-facing)
* tests (always)

Docs should explain **why**, not just **what**.
